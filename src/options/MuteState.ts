import { CompanionInputFieldCheckbox } from '@companion-module/base';

const muteStateOption: CompanionInputFieldCheckbox = {
    id: 'muteState',
    type: 'checkbox',
    label: 'Set Muted',
    default: true,
};

export default muteStateOption;
