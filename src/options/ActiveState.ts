import { CompanionInputFieldCheckbox } from '@companion-module/base';

const activeStateOption: CompanionInputFieldCheckbox = {
    id: 'activeState',
    type: 'checkbox',
    label: 'Is Active',
    default: true,
};

export default activeStateOption;
